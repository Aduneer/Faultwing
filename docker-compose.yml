name: faultwing

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: faultwing
      POSTGRES_USER: faultwing
      POSTGRES_PASSWORD: faultwing
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
